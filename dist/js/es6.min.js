'use strict';

var foo = 'outer';
var f = function f(x) {
	return foo;
};

!(function bar() {
	var func = arguments.length <= 0 || arguments[0] === undefined ? f : arguments[0];

	var foo = 'inner';
	console.log(func()); // outer
})();
alert('tan');